<% if @category.errors.any? %>
  <div id="error_explanation">
    <div class="title1">
      <%= pluralize(@category.errors.count, "error") %> prevented your category from being saved:
    </div>
    <ul>
    <% @category.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>