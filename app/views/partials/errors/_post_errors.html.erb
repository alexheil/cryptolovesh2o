<% if @post.errors.any? %>
  <div id="error_explanation">
    <div class="title1">
      <%= pluralize(@post.errors.count, "error") %> prevented your post from being saved:
    </div>
    <ul>
    <% @post.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>