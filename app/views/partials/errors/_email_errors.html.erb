<% if @global_email.errors.any? %>
  <div id="error_explanation">
    <div class="title1">
      <%= pluralize(@global_email.errors.count, "error") %> prevented your email from being saved:
    </div>
    <ul>
    <% @global_email.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>